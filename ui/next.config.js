/** @type {import('next').NextConfig} */
const nextConfig = {
  // Point this at Teammate B's orchestrator when /run is live.
  // env: { ORCHESTRATOR_URL: "http://localhost:8000" },
};
module.exports = nextConfig;
